<div class="content" style="color:white;">
<h1 style="color:white;">Favourites</h1>
{{#if errors.length}}
	<p>Oh no, something went wrong:</p>
	<ul>
		{{#each errors}}
			<li>{{this}}</li>
		{{/each}}
	</ul>
{{else}}
	{{#if movies.length}}
		<ul>
			{{#each movies}}
                <div class="box">
<!--                    {{movieId}}-->
                    {{movieTitle}}
                </div>
			{{/each}}
		</ul>
	{{else}}
		<p>No favourites exist yet.</p>
	{{/if}}
{{/if}}
</div>